extends layout.pug 

block content 
    h1 #{err.status} - #{err.message}

    p Click the link below to return to the Home page.

    h6 
        a(href='/') HOME

    h6 Error Stack:
    
    p #{err.stack}