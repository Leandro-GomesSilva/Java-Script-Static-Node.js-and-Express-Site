extends layout.pug 

block content 
    h1 #{err.status} - #{err.message}

    h3 Click the link below to return to the Home page.

    h2 
        a(href='/') Home

    h3 Error Stack:
    
    p #{err.stack}